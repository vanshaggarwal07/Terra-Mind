# twin-cv

Phase 4 satellite change-detection. Pulls Sentinel-2 / Bhuvan tile pairs,
runs semantic segmentation to flag new built-up area, and emits
`construction_detected` pattern signals (lower-trust tier).

Built across **P4.3 - P4.5**.
