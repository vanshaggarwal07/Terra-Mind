# twin-simulation

Phase 4 what-if engine. Applies scenario overrides to `InfraEvent` state in a
sandbox (never persisted), recomputes the Phase-3 models + Future Intelligence
Score, and diffs against baseline. Built across **P4.1 - P4.2**.
