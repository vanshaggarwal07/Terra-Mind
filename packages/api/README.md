# twin-api

The product/API layer (FastAPI). Serves the Digital Twin facts, the Future
Intelligence Score, the RERA builder card, proximity cards, the RAG copilot,
and the ML prediction endpoints.

Routers are added across Phases 1-3. Run with `make api`.
