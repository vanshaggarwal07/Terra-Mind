# twin-common

Shared library used by every service: typed config, structured logging, DB
session management, S3-compatible object storage, a resilient HTTP client
(rate limiting, backoff, honest user-agent, robots.txt, conditional requests),
and content hashing.

Built in prompt **P0.3**.
